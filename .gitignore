.env
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
# Regenerated by `pip install -e .` - editable-install metadata, not source.
*.egg-info/
build/
dist/
.idea/
.vscode/

# Raw API captures. Kept locally as the recovery path for parser bugs, but
# not committed: they grow without bound and contain no logic.
raw/
